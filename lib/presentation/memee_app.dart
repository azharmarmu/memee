import 'package:flutter/material.dart';

class MemeeApp extends StatelessWidget {
  const MemeeApp({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}